# Copy one of the following templates to the repository root:
# - deploy/.env.staging.example -> .env.staging
# - deploy/.env.prod.example -> .env.prod
#
# Do not commit real credentials or private keys.
